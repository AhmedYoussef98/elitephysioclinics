VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Serverless (set in the Netlify dashboard, not committed):
# RESEND_API_KEY   — Resend API key for booking notification emails
# SUPABASE_URL / SUPABASE_ANON_KEY — used by the email function to verify bookings
#   (falls back to the VITE_-prefixed values above if unset)
